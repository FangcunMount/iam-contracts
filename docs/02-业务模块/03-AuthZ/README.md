# AuthZ：直接角色与资源动作授权

> 状态：已实现 · 本轮代码已实现，生产数据切换与业务验收另行记录。

AuthZ 根据 Subject 的直接 Assignment 找到角色，将多个角色的 Grant 取并集，再匹配 Resource 与 Action。没有匹配授权时默认拒绝。角色继承和对象条件已从活动实现移除。

业务系统负责机构、监护、医生管理关系、对象状态和数据查询范围。IAM 不存放这些关系，也不通过角色名称推断它们。Scope 建模属于独立后续工作。

| 主题 | 文档 |
|---|---|
| 模块职责 | [模块总览](00-模块总览.md) |
| 领域对象与关系 | [领域模型](01-领域模型设计.md) |
| 判定、快照与失败边界 | [授权判定](02-关键链路-授权判定与不可变快照.md) |
| 写入、事务和服务授予边界 | [授权写入](03-关键链路-授权写入与受管Assignment.md) |
| 版本与 Outbox | [多实例收敛](04-关键链路-多实例策略收敛.md) |
| 管理接口 | [REST 管理](05-关键链路-REST管理与路由授权.md) |
| 服务调用合同 | [gRPC 与 SDK](06-关键链路-gRPC服务间授权与SDK.md) |
| 业务责任 | [模块边界](07-模块边界-AuthZ与AuthN-Identity-Suggest.md) |
| 实现位置 | [代码索引](08-分层架构与代码索引.md) |
| 条件数据迁移与发布 | [退役维护手册](08-条件授权退役维护手册.md) |
| 安全验证 | [加固与验收](09-安全加固与发布验收.md) |

当前代码状态与生产切换状态分别记录。最终版本不能在残留有效条件 Grant 或非空资源属性模式的数据库上就绪；必须先按维护手册完成迁移。

`ReplaceManagedAssignments` 只替换调用服务获准管理的角色集合，保留集合之外的分配；方法 ACL 与服务角色白名单共同生效。授权快照的 `direct_roles` 表示直接分配，不包含继承。
