# 遗留资产、兼容层与数据库退役审计

> 状态：已实现 · `000019–000029` 已完成生产验收或受控收尾，目标库为 15 张运行表加 `schema_migrations`。000028 只增加 AuthN 唯一索引，000029 只退役 JWKS action，均不新增数据表。AuthZ 字符串 Scope、
> 持久化 Casbin 权限事实和一次性切换入口均已退役；历史 migration 与旧对象缺失性门禁继续保留。本文是遗留资产退役的 canonical 台账。

## 1. 结论先行

IAM 的六项仓库内遗留代码已经退役，schema/bootstrap 双真相源也已收敛；历史 MySQL 表、外部兼容和治理缺口仍不能按名字统一处理。

| 结论 | 当前判断 |
| --- | --- |
| 业务模块 | 仍以 3 个核心模块 Identity/AuthN/AuthZ 和 2 个辅助模块 IDP/Suggest 为边界 |
| 已完成代码清理 | AuthZ 根包 facade、旧 Profile Editor、旧 OTP code store、`OrgIDOrDefault`、无调用 JWKS helper、失效注释和 Makefile 提示均已移除 |
| 必须保留的兼容 | REST `wechat` wire 名、`/health`、历史数值 `tenant_id` 归一、历史事件 payload、Suggest 删除配置 tombstone |
| MySQL 表 | 15 张是当前运行表，1 张是迁移元数据；`000019–000029` 已分批移除历史、无适配器、一次性清理副本、旧 AuthZ 权限事实和废弃 JWKS action，并建立 AuthN 全局标识唯一索引；生产精确为 16 张，000028–000029 不改变表数量 |
| AuthN 退役 | `auth_accounts/auth_credentials_legacy` 已删除；canonical 缺失为 0，旧 JOIN 本就不可达的 3 条密码和 5 条 OAuth 只保留在发布前完整备份中，不重新 merge |
| 已消除中间态 | bootstrap 的 AuthZ 域已统一为 `fangcun`；静态 schema 快照已移除；迁移链成为 schema 唯一事实源并有门禁 |
| 事件/缓存 | 一般 Outbox backlog 已进入 required readiness；Suggest Redis/内存限流已作为互斥可选 family 进入 cache catalog 与运行状态聚合 |
| 审计表 | 三表生产精确行数均为 0，无 writer/reader 和数据库对象依赖；业务所有者已确认 MySQL 不承担该真相源，结构化应用日志保留，生产从未装配的领域 `AuditLogger` 休眠端口已删除 |

退役状态只使用以下五类：

| 状态 | 含义 | 当前允许的动作 |
| --- | --- | --- |
| 已完成 | 删除责任链、行为保护、文档与验证已经闭合 | 保留防回流门禁，后续按回归处理 |
| 保留兼容 | 已有调用方、部署或显式兼容测试 | 不删除；补充到期条件和观测 |
| 待生产证据 | 静态证据很强，但缺真实流量/数据/配置证据 | 只做只读预检和观测 |
| 待产品决策 | 删除会放弃尚未闭合的业务/合规能力 | 先确认能力归属和保留期 |
| 治理缺口 | 当前能力的可靠性/可观测性不足 | 独立设计，不混入遗留清理 |

## 2. 审计范围与证据边界

本次按业务和技术两个方向交叉检查：

```text
业务方向
  Identity / AuthN / AuthZ
  IDP / Suggest

技术方向
  REST / gRPC / Go SDK
  composition root / config / deployment
  MySQL / migration / bootstrap
  Redis / cache governance
  event catalog / Outbox / readiness
  tests / architecture guards / cross-repository callers
```

证据包括当前代码、路由注册、OpenAPI/proto、SDK、配置、迁移、脚本、测试、Git 历史、本地 MySQL 验证和目标库工作流证据。
已取得 `children/guardianships/schema_version/tenants/data_dictionary/operation_logs/audit_logs/auth_token_audit` 的分批退役授权，
8 张表均已在生产删除并通过独立验收。

命名中出现 `legacy`、`deprecated` 或 `v1` 只生成候选，不构成删除授权。反过来，没有旧名字的 `schema_version`、`tenants` 等表也可能是遗留资产。

## 3. 五个业务模块的排查结果

| 模块 | 当前主链路 | 遗留/中间态 | 结论 |
| --- | --- | --- | --- |
| Identity | `users`、`profiles`、`profile_links`、Session 吊销 Outbox | 旧 Profile Editor 和 `children/guardianships` 已删除 | 当前 MyProfiles Patch 保持；生产已验收 `000019` |
| AuthN | `auth_login_identities`、`auth_credentials`、JWKS、Redis Session/Challenge | 旧 OTP code store 与 `auth_accounts/auth_credentials_legacy` 已删除 | Challenge、send gate、quota 保持；生产已验收 `000023`，无需重新 merge 旧表数据 |
| AuthZ | 角色、资源、分配、继承、PermissionGrant、策略版本和 durable version event | 仓库与生产均只使用 v3；字符串 Scope、持久化 Casbin 权限事实和切换工具已退役 | 保留历史 migration 与旧对象缺失性门禁，禁止恢复兼容入口 |
| IDP | 微信应用、SecretVault、远端 token cache | 旧 AuthN 登录路由注释已清理；缓存本身仍活跃 | 不删除 IDP 表和缓存 |
| Suggest | 从 `profiles` 派生的进程内读模型、可见性过滤、Redis/内存限流 | `profiles` 无 `org_id`，loader 用 placeholder org 和 `created_by`；未启用的数值 `TenantID/TenantIDs` 字段已删除 | 保留字符串 `TenantDomain`、业务 `OrgID/OrgIDs` 与 owner/visibility 范围 |

Suggest 没有独立 MySQL 主表；它的索引可重建，但可见性规则、手机号脱敏和限流不是可随索引一起删除的“缓存细节”。

## 4. 代码、接口与配置退役台账

### 4.1 已完成

| ID | 结果 | 行为保护 | 验证 |
| --- | --- | --- | --- |
| RET-CODE-001 | 删除根级 AuthZ facade 及只服务于 facade 的测试 | 生产和测试均直接使用语义子包；架构测试禁止根包 import 回流 | AuthZ domain 与 architecture 聚焦测试 |
| RET-CODE-002 | 删除旧 Profile Editor、专属 DTO/接口和旧测试 | `MyProfiles.Patch`、User Editor、REST/gRPC 装配保持 | Identity profile、container、REST/gRPC 聚焦测试 |
| RET-CODE-003 | 删除 `OTPCodeStore`、旧 Put/Delete/VerifyAndConsume、旧 key builder 与专属测试 | ChallengeRepository、send gate、quota 保持 | Redis、AuthN challenge、container 聚焦测试 |
| RET-CODE-004 | 删除 `OrgIDOrDefault` | `BusinessOrgID` 无 claim 时仍返回 `ok=false` | requestctx 聚焦测试 |
| RET-CODE-005 | 清理旧登录路由说明、失效 db-init 提示和 `deploy-prepare` target | `db-bootstrap`、deploy/CD 和 canonical authn 路由保持 | route contract、IDP container、Make help 检查 |
| RET-CODE-006 | 删除只存在于自述文档、无接口/调用方的 internal `GetJWKSStats/JWKSStats`、`GetCacheControl`、`ValidateJWKS` | `BuildJWKS`、`SnapshotStatus`、缓存标签、轮换后刷新和管理员发布链路保持 | keyset、architecture、全量测试 |

### 4.2 Batch C 退役结果

| ID | 候选 | 当前证据 | 退役前还缺什么 |
| --- | --- | --- | --- |
| RET-API-001 | ProfileLink REST `active` query | 已删除兼容映射；旧 query fail closed，`include_revoked` 是唯一 wire 契约 | 已完成 |
| RET-CONFIG-001 | `loader_placeholder_tenant_id` | 已删除 fallback；旧 YAML/环境变量 fail closed，使用 `loader_placeholder_org_id` | 已完成 |
| RET-EVENT-001 | `sms.mq.topic` 与 `NewMQLoginOTPSender` legacy publisher | 已删除；MQ 短信只使用 catalog publisher 和固定 `iam.notify.sms` 事件路由 | 已完成 |
| RET-RUNTIME-001 | `ModuleStatus` 旧 bool 字段 | 已删除；`ModuleState` 与 `module_states` 为唯一事实源 | 已完成 |
| RET-SDK-001 | `TokenClaims.TenantID`、`JWKSStats` | `v2.0.10` 已发布标准 Go `Deprecated:` 契约；已知消费仓无精确符号引用；`/v3` module 已删除，canonical 替代保留 | 已完成；v2.0.10 继续可用，v3 消费者只迁 import path 和 canonical API |
| RET-SUG-001 | Suggest 的数值 `TenantID/TenantIDs` 过渡字段 | 已删除；授权域仅使用 `TenantDomain`，数据可见性使用 `OrgIDs`/owner/explicit ProfileIDs | 已完成 |

### 4.2.1 C1 观测 waiver 与 C2 删除

2026-08-12，业务所有者明确免除 Batch C 的 30 天运行时观测期，并授权直接进入 C2。代码因此删除上述四项运行时兼容面，同时退役临时 C1 指标、30 天 artifact 生成器和显式 readiness gate，
避免留下新的中间态工具链。

waiver 不改变公开 SDK 的 Go module major-version 边界：`TokenClaims.TenantID` 和 `JWKSStats` 在 `v2.0.10` 仍保留，
当时的代码通过 `github.com/FangcunMount/iam/v3` 新 module 删除；REST/gRPC wire 仍保持 v2。

发布后复扫发现镜像/ Makefile 的 linker 参数曾写入不存在的 `main.Version/BuildTime/GitCommit`，而 `/version` 读取的是 `pkg/version`；
该发布中间态已改为注入 `pkg/version` 中的 `GitVersion`、`BuildDate`、`GitCommit`。定时巡检继续要求 `/version.gitCommit` 等于容器镜像的完整 SHA tag。

扫描快照记录在 [`compat-consumer-evidence.yaml`](compat-consumer-evidence.yaml)。
`qs-server` 与 `seeddata-runner` 已分别通过 PR #7/#4 迁移至 `v3.0.0`，只改 Go module/import path，
REST URL 和 gRPC v2 wire contract 不变；`qs-collection-system` 对自身调用方保留 `active` 入参，但发往 IAM 时只使用 `include_revoked`。
这些是指定 SHA 上的静态与 CI/CD 证据，不代表未列出的私有 fork 或外部 SDK 消费者。

2026-08-12 发布后再用 deadcode RTA（包含测试根）复扫：剩余 RTA internal 项是活跃接口所需的 `SupportedAlgorithms` 和用于实现完整 repository 接口的测试 stub。
补充精确符号调用扫描后发现 RTA 未报告的三个 `KeySetBuilder` 辅助面只出现在实现与自述文档中，未进入接口、容器、handler 或测试调用链，已删除并加架构防回归。
大量 `pkg/sdk` 导出 API 被 RTA 标为本仓不可达，但 `qs-server` 和 `seeddata-runner` 存在真实 package 导入；
v3 仅删除已在 `v2.0.10` 正式弃用且精确消费扫描为零的两个符号，其余公开 RTA 候选继续保留。`pkg/app`、`pkg/auth`、`pkg/flag`、`pkg/uow` 的部分导出符号未发现指定工作区外部引用，也继续保留，不能仅凭本仓 RTA 删除。

### 4.3 当前必须保留

| 兼容面 | 保留原因 | 退役触发条件 |
| --- | --- | --- |
| REST `auth_method: "wechat"` -> 内部 `wechat_mini` | OpenAPI、契约测试和已发现的外部业务系统仍使用 wire 名 `wechat` | 新 wire 契约发布、调用方迁移、流量归零 |
| `/health` | `configs/nginx/conf.d/iam.fangcunmount.cn.conf` 和 README 仍使用；`/healthz`/`/readyz` 承担不同探针语义 | 先迁移 nginx 和所有调用方，再经过发布窗口 |
| 历史数值 JWT `tenant_id` | 旧 token 仍可能携带 `1`；当前代码显式归一到 `fangcun`，且不从中推导业务 org | 最长 token TTL 过去且签发链路确认不再产生数值值 |
| 历史事件 payload shape | 事件可重放，消费者不全在本仓库；兼容测试保护旧 shape | 事件版本化、积压清空、消费者升级 |
| `RemovedDataDir` / `RemovedSnapshot` | decode-only tombstone 会对已删除配置 fail closed，防止静默忽略 | 生产配置盘点和至少一个完整发布窗口后再删 |

## 5. MySQL 表清单与分类

用户提供的数据库截图包含 25 张表。按当前运行时适配器和迁移用途分类如下。

### 5.1 当前运行表：15 张，保留

| 责任 | 表 |
| --- | --- |
| Identity | `users`、`profiles`、`profile_links` |
| AuthN/JWKS | `auth_login_identities`、`auth_credentials`、`jwks_keys` |
| AuthZ | `authz_resources`、`authz_roles`、`authz_assignments`、`authz_role_inheritances`、`authz_permission_grants`、`authz_policy_versions` |
| IDP | `idp_wechat_apps` |
| 可靠异步 | `domain_event_outbox`、`identity_session_revocation_outbox` |

`domain_event_outbox` 和 `identity_session_revocation_outbox` 都是持久任务事实，不是可随时清空的缓存。

### 5.2 迁移元数据：1 张，保留

`schema_migrations` 是 golang-migrate 的当前版本/dirty 状态表，`internal/pkg/migration/migrate.go` 显式指定该表。不要把它和业务遗留表一起删除。

### 5.3 明确历史业务表：Identity/AuthN 共 4 张均已退役

| 表 | 当前仓库用途 | 状态与退役门 |
| --- | --- | --- |
| `children` | 000002 历史桥接；000019 只补缺失 `profiles`、严格对账后删除 | 生产已验收 `version=19, dirty=0` 且表不存在 |
| `guardianships` | 000002 历史桥接；000019 只补缺失 `profile_links`、严格对账和依赖检查后删除 | 生产已验收 `version=19, dirty=0` 且表不存在 |
| `auth_accounts` | migration 000011 和退役期 format v5 reconciliation/preflight 识别 | 230,093 行均已有 canonical 映射；生产已验收 `version=23, dirty=0` 且表不存在 |
| `auth_credentials_legacy` | migration 000011 和退役期 format v5 reconciliation/preflight 识别 | 可达数据已 canonical；3 条密码与 5 条 OAuth runtime-unreachable 只由发布前备份承接，表已删除且不重新 merge |

`000023` 的 DROP 清单只有 `auth_credentials_legacy, auth_accounts`。它不修改 canonical 数据，先强制检查旧表成对状态、canonical schema、
数据收敛和数据库对象依赖，再执行一条最终 DROP；删除授权来自本次显式发布决策，不来自表名中的 `legacy`。

### 5.4 无当前生产读写适配器：6 张，分两类决策

| 表 | 当前事实 | 分类 |
| --- | --- | --- |
| `schema_version` | 只有历史 DDL/seed；真实 migrator 使用 `schema_migrations` | 生产已执行 `000020`，验收 `version=20, dirty=0` 且表不存在 |
| `tenants` | 无当前 repository；当前 bootstrap 写入已移除 | 生产已执行 `000021`，验收 `version=21, dirty=0` 且表不存在 |
| `data_dictionary` | 无当前 repository；当前 bootstrap 写入已移除 | 生产已执行 `000021`，验收 `version=21, dirty=0` 且表不存在 |
| `operation_logs` | 有历史 DDL，无当前 writer/reader；发布前生产 0 行、零对象依赖 | 生产已执行 `000022`，验收表不存在 |
| `audit_logs` | 有历史 DDL；领域 `AuditLogger` 无 MySQL adapter/wiring；发布前生产 0 行、零对象依赖 | 生产已执行 `000022`；只删物理表，结构化日志和审计端口保留 |
| `auth_token_audit` | 有历史 DDL，无 Token 签发/撤销 writer；发布前生产 0 行、零对象依赖 | 生产已执行 `000022`，验收表不存在 |

`000022` 只包含 `operation_logs/audit_logs/auth_token_audit`；AuthN 两表由独立 `000023` 处理。目标库 fresh verify 若不再 eligible、
出现非零 I/O 或数据库对象依赖，仍须停止发布，不能由 waiver 覆盖。

### 5.5 一次性 seeddata 清理副本：4 张已由 000024 退役

| 表 | 生产事实 | 结论 |
| --- | --- | --- |
| `cbpt_profiles_s812v2` | 1,359 行；与 `cleanup_bak_*profiles*` 结构、扩展 checksum、ID 集和逐字段内容一致；canonical ID 重叠 0 | 一次性清理备份，删除，不回填 |
| `cbpt_profile_links_s812v2` | 1,359 行；与 `cleanup_bak_*profile_links*` 结构、扩展 checksum、ID 集和逐字段内容一致；canonical ID 重叠 0 | 一次性清理备份，删除，不回填 |
| `cleanup_bak_perf_testee_profiles_seeddata_dup_20260812_v1` | 与对应 `cbpt_*` 副本相同，创建时间早 9 分钟 | 重复备份，删除 |
| `cleanup_bak_perf_testee_profile_links_seeddata_dup_20260812_v1` | 与对应 `cbpt_*` 副本相同，创建时间早 9 分钟 | 重复备份，删除 |

四表均无当前仓库精确引用，也没有 FK、trigger、view、routine、event 依赖。跨仓复扫确认 `qs-server` one-off 工具会按参数动态生成 `cbpt_<table>_<suffix>`
作为显式清理备份，但不是运行时自动 writer，也没有固化本次 suffix；该工具仍有受控清理用途，不能仅因这四张历史产物删除。发布后 IAM `status` 的 15 表精确白名单会让任何未来备份对象显式暴露，
防止再次静默形成 schema 中间态。

## 6. Schema 与发布链路中的中间态

### 6.1 已发布 migration 可变

当前 000002 自己注明它要桥接旧 `children/guardianships`，因为 000001 在 Identity 重构后被编辑。迁移版本只执行一次；
同样的 `schema_migrations.version=2` 可能对应不同物理结构。

本地 `iam_contracts` 快照印证了这一风险：版本为 `2, dirty=0`，但没有 `profiles/profile_links`，仍有 `children/guardianships`。这不证明生产状态，
只证明“版本号相同”不能证明“当前文件内容执行过”。

从现在开始必须执行：

- 000001–000019 视为已发布且不可再改；
- 000020/000021 是独立的 forward contract migration，发布后同样不可原地修改；
- 后续修复继续新增 forward migration；
- migration acceptance 同时检查 version、列/索引/约束和关键数据，不只检查 `dirty=0`；
- 不通过手改 `schema_migrations` 伪造执行完成。

### 6.2 AuthZ tenant domain 双源冲突

migration 000014 曾把旧授权表中的历史 `"1"` 归一到 `"fangcun"`；这些旧权限事实只作为 000026/离线转换的历史输入，并由 000027 删除。

该中间态已消除：`configs/mysql/bootstrap.sql` 的 QS 角色、分配、policy 和策略版本现在只写 `fangcun`。
`TestAuthzStandaloneBootstrapUsesCanonicalTenantDomain` 会阻止 AuthZ seed 重新出现 `"1"`；
AuthN login identity 的历史 realm 不是 AuthZ tenant domain。`data_dictionary` seed 已随 000021 contract 从当前 bootstrap 移除，
历史 JWT 数值兼容继续由 token 归一层处理。

### 6.3 Schema 唯一事实源

当前只保留两类不同职责的输入：

```text
internal/pkg/migration/migrations/*.sql   schema 唯一事实源
configs/mysql/bootstrap.sql               schema 当前后可执行的幂等基线数据
```

没有真实执行入口且已与 migration 终态漂移的静态 schema 快照已经删除。`configs/mysql/README.md` 记录职责，
`scripts/check-docs-facts.py` 同时阻止静态副本回归和 bootstrap 混入 DDL。`make db-bootstrap` 保留，
但只能在应用完成 migration 且 `schema_migrations` clean 后使用。

## 7. 事件、缓存与审计的邻接问题

这些问题在排查中发现，但不应伪装成“删遗留代码”一并处理。

| ID | 发现 | 风险 | 分类 |
| --- | --- | --- | --- |
| GOV-EVENT-001 | `domain_event_outbox` 的 unfinished bucket 由 required readiness 检查 | 最老记录超过统一 Outbox 阈值时退出 ready，避免 EventBus/relay 故障被启动成功掩盖 | 已治理 |
| GOV-CACHE-001 | cache catalog 登记 13 个 family，composition 汇总 AuthN/IDP/Suggest inspector | Suggest Redis/内存限流按实际启用后端报告状态；未启用的互斥 family 不误判后端不健康 | 已治理 |
| GOV-CACHE-002 | 旧 `otp:{scene}:{phone}:{code}` code family 不在 catalog | 进一步支持旧 OTP code store 已退出主链路 | RET-CODE-003 的佐证 |
| GOV-AUDIT-001 | MySQL 审计 DDL 无实际 writer/reader；领域 `AuditLogger` 也从未在生产组合根装配，当前运行审计依赖结构化应用日志 | `000022` 删除空物理表及后续删除休眠端口都不影响结构化日志，但也不会补齐不可篡改、长期留存等合规能力 | 休眠端口已删除；合规能力另行治理 |

一般 Outbox 与 Suggest cache governance 已按现有运行参数闭合：前者复用 `outbox_max_pending_age`，后者保持限流 fail-open 语义并暴露实际后端健康。不可篡改、
长期留存的审计能力仍是独立产品与合规决策，不能从结构化日志自动推导。

## 8. 数据库退役的强制门槛

任何候选表只有同时满足以下条件，才允许新增 forward `DROP TABLE` migration：

1. **仓库零引用**：运行时、脚本、job、bootstrap、报表、测试 fixture 和文档不再把它当事实源；
2. **跨仓零引用**：业务系统、seed/运维仓库和 SDK 无直连 SQL/接口依赖；
3. **目标库零读写**：默认应覆盖业务周期；本轮用户选择即时预检，必须记录计数器 uptime/采集时间并接受无法排除低频外部查询的剩余风险；
4. **数据一致性**：旧表到 canonical 表的主键、状态、软删除、关系和关键字段完成逐项对账；
5. **写入已停止**：先发布停止旧写/回填工具的版本，再经过观察窗口；
6. **依赖清空**：外键、trigger、view、event、procedure、账号权限和离线任务都已盘点；
7. **备份可恢复**：备份文件存在不够，需隔离恢复并完成关键数据断言；
8. **forward-only**：用新 migration 删除，不修改已发布版本；000019–000024 的 down 均 fail closed，不把 destructive down 当作可靠回滚。唯一例外是 2026-09-04 对从未能在 canonical schema 成功执行的 000029 错误列名补丁，事故、备份、受控收尾和回归测试均已入账；
9. **分批发布**：一次只退役一个同责任组，先 canary/单环境，再扩大；
10. **证据入账**：在本文记录环境、SHA、时间窗口、查询摘要、操作者和恢复证据链接。

format v5 退役预检、Identity retire、AuthN reconcile/verify/apply、旧 AuthZ cutover 和早期 oneoff 均属于已完成生命周期的一次性工具，
现已从 IAM 仓库运行入口和生产镜像中移除。当前 `Database Operations/status` 保留 secret-safe 的历史对象防回归检查：`version=29, dirty=0`、迁移锁状态、
16 张 BASE TABLE 精确白名单，以及全部历史退役表和显式表级授权缺席。[000029 受控收尾前状态](https://github.com/FangcunMount/iam/actions/runs/33861927119)
与[完整备份](https://github.com/FangcunMount/iam/actions/runs/33862117713)是本轮证据入口；Assignment guard 另由历史命名的 preflight 持续检查重复组为 0 且生成列/复合唯一索引为 `1/1`。

仓库现在另有 `iam-maintenance authz-v3-converge` 与 `.github/workflows/authz-v3-converge.yml`。
它面向当前 AuthZ v4 数据模型执行 preflight/apply/verify/evidence，不是旧 v2→v3 cutover 的恢复，也不改变上述退役结论。

## 9. 实施顺序

```text
Batch 0  已完成：固化 baseline、停止修改旧 migration
  -> Batch 1  已完成：修复 bootstrap/schema/Makefile 的 truth-source 漂移
  -> Batch 2  已完成：删除 RET-CODE-001..005，每项独立验证
  -> Batch 3  工具与生产只读运行已完成：AuthN 表 I/O 不可见，owner 已显式批准剩余风险
  -> Batch 4  只退役证据闭合的 API/config/SDK 兼容
  -> Batch 5A Identity 已完成生产验收：000019 退役 children/guardianships
  -> Batch 5B 已完成生产验收：000020 单独退役 schema_version，version=20 dirty=0
  -> Batch 5C 已完成生产验收：当前 bootstrap 停止写 tenants/data_dictionary，000021 单独退役两表，version=21 dirty=0
  -> Batch 5D 已完成生产验收：format v5 收敛后由 000023 退役 AuthN 两表，version=23 dirty=0
  -> Batch 5E 已完成生产验收：000022 只退役 operation_logs/audit_logs/auth_token_audit，version=22 dirty=0
  -> Batch 5F 已完成生产验收：000024 退役四张 seeddata 清理副本，version=24 dirty=0 / 15 tables
  -> Batch 5G 已完成生产验收：清理冗余 active RoleBinding 后由 000025 建立数据库唯一 guard，version=25 dirty=0
  -> Batch 5H 已完成生产验收：000026 转换到 PermissionGrant/RoleInheritance，000027 退役旧 AuthZ 表列，version=27 dirty=0 / 16 tables
  -> Batch 5I 已完成生产验收：000028 收敛 AuthN global_identifier 并建立唯一索引，version=28 dirty=0 / 16 tables
  -> Batch 5J 已完成受控收尾：000029 退役 JWKS enter_grace action，version=29 dirty=0 / 16 tables
```

Batch 5A–5J 均已完成目标库验收；一次性退役入口没有恢复，日常精确 schema 白名单持续防止备份对象静默回流。

## 10. 审计基线证据

2026-08-07 在提交 `41a7d7f3eeb24970d51201f2bc42144db7b92bee` 上执行并通过：

```bash
go test ./...
go vet ./...
python3 scripts/check-route-contracts.py
python3 scripts/check-docs-facts.py
make docs-hygiene
```

另用 deadcode RTA（包含测试根）生成 59 个不可达 symbol 作为候选输入。deadcode 结果不等于安全删除结论；反射、生成代码、外部包 API 和 build tag 仍需人工判断。

同日只读观察本地 Docker MySQL 的 `iam_contracts`：

| 项目 | 结果 |
| --- | --- |
| migration | `version=2, dirty=0` |
| 表数/形态 | 19 张；仍有 `children/guardianships/auth_accounts`，没有 `profiles/profile_links` |
| `children` | 18,940 行 |
| `guardianships` | 24,964 行 |
| `auth_accounts` | 3 行 |
| `auth_credentials` | 3 行 |
| `users` | 19,973 行 |

这是本地合同测试库，不是生产证据；它只用于证明 migration 历史漂移风险。

首批退役在同一基线后的未提交工作树中完成，并通过：

```bash
go test ./...
go vet ./...
go test -v ./internal/pkg/migration -count=1
go test ./scripts/dbops -count=1
python3 scripts/check-route-contracts.py
python3 scripts/check-docs-facts.py
make docs-hygiene
make docs-facts
bash -n scripts/dbops/legacy-retirement-preflight.sh
git diff --check
```

Migration 静态契约测试通过；两个 MySQL-backed migration semantics 测试因未提供 `MYSQL_HOST` 跳过。本批没有修改 000001–000018，也没有执行生产数据库预检、
写型 MySQL integration 或任何候选表删除，因此不能把上述结果外推成生产退役验收。

随后在提交 `32860c6878a49aab35f651ff6b340f8575a25993` 后的工作树中实施 Identity 表 contract 批次：

- 新增 `000019_retire_legacy_tables`，只包含 `children/guardianships`；
- `INSERT IGNORE` 只补 canonical 缺失记录，冲突时不覆盖当前 `profiles/profile_links`；
- 数据对账和 FK/trigger/view/routine/event 依赖断言均位于唯一最终 DROP 之前；
- MySQL 8 合成测试覆盖正常删除、canonical 冲突保留旧表、View 依赖保留旧表和 down fail-closed 四条路径；
- 把本地 `iam_contracts` 的 18, 940 条 `children` 和 24, 964 条 `guardianships` 只读克隆到临时库演练，得到 18, 940 条 `profiles`、
  24, 964 条 `profile_links`，两张旧表不存在；临时库随后清理，原 `iam_contracts` 未修改。

以上是本地验证，不是生产执行证据。生产完成的最小判据是：备份可恢复、单执行者成功运行、`schema_migrations(version=19, dirty=0)`、两张 canonical 表验收通过且旧表不存在。

随后在 `37e98069a5ec236b5e17d794825452adb2bd5097` 基线上实现第二阶段 repository 批次：

- format v2 在候选表读取前缓存 Performance Schema I/O，随后输出精确行数、schema 指纹、增强 AuthN parity 和 eligibility；
- `000020` 只处理 `schema_version`，`000021` 只处理已从当前 bootstrap 移除的 `tenants/data_dictionary`；两个 down 均 fail closed，
  三张日志/审计表没有进入 migration；
- MySQL 8 临时空库完整升级到 `version=21, dirty=0`，确认 5 张已授权旧表不存在、三张日志/审计表保留，当前 bootstrap 连续执行两次成功；临时库随后删除；
- MySQL 8 合成 AuthN 数据执行 oneoff apply 后，account、password、Phone 和 OAuth artifact parity 全部为零缺失/不匹配/重复；该结果只验证查询语义，
  不是目标库 backfill 证据；
- 本地 `iam_contracts` 的 format v2 结果仍为 `version=2, dirty=0`，`children/guardianships/auth_accounts` 有读取计数，
  `tenants` 也有即时读取计数，canonical AuthN 表缺失；eligibility 因版本、I/O 和旧 schema 形态 fail closed，因此当时没有创建 AuthN 退役 migration。
  后续生产 format v5 收敛和本次显式授权改变了这一边界。

本批执行并通过：

```bash
go test ./...
go vet ./...
go test ./scripts/dbops ./scripts/oneoff ./internal/pkg/migration -count=1
python3 scripts/check-route-contracts.py
python3 scripts/check-docs-facts.py
make docs-hygiene
bash -n scripts/dbops/legacy-retirement-preflight.sh
git diff --check
```

上述是仓库与本地语义验证，不单独代表生产执行。后续已按独立发布点取得以下目标环境证据：

- `000019`：独立生产发布后达到 `version=19, dirty=0`，`children/guardianships` 不存在；
  [发布记录](https://github.com/FangcunMount/iam/actions/runs/31166091205)、
  [发布后只读验收](https://github.com/FangcunMount/iam/actions/runs/31166501679)。
- `000020`：[PR #4](https://github.com/FangcunMount/iam/pull/4) 独立合并；
  [生产只读预检](https://github.com/FangcunMount/iam/actions/runs/31170913104) 在零数据库对象依赖下由 owner I/O waiver 判定
  `schema_version` eligible；[发布前备份](https://github.com/FangcunMount/iam/actions/runs/31171022571) 的时间戳为
  `20260807_184053`，大小 32, 733, 616 bytes；[生产发布](https://github.com/FangcunMount/iam/actions/runs/31171520383) 使用 merge
  SHA `bed7e8679b46d27ba9c0d994fa8ed06e37b09c48`；[发布后只读验收](https://github.com/FangcunMount/iam/actions/runs/31171885003)
  证明 `version=20, dirty=0`、`schema_version` 不存在，目标库共 22 张表。
- `000021`：[生产只读预检](https://github.com/FangcunMount/iam/actions/runs/31172035361) 在 `version=20, dirty=0` 上得到
  `tenants=2` 行、`data_dictionary=13` 行，两表 FK/trigger/view/routine/event 依赖均为 0；Performance Schema 表 I/O 不可用，
  两表依用户的 `platform_tables` owner waiver 判定 eligible。[PR #5](https://github.com/FangcunMount/iam/pull/5) 的 MySQL 8
  门禁覆盖单版迁移和空库完整升级/ bootstrap；[发布前备份](https://github.com/FangcunMount/iam/actions/runs/31172886666) 的时间戳为
  `20260807_190849`，大小 32, 738, 799 bytes；
  [main MySQL 8 复验](https://github.com/FangcunMount/iam/actions/runs/31172983279) 和
  [main CI](https://github.com/FangcunMount/iam/actions/runs/31172983360) 成功后，
  [生产发布](https://github.com/FangcunMount/iam/actions/runs/31173306116) merge SHA
  `f1f90df09fc0fed6eb20f168b0bce616858016f3`；[发布后只读验收](https://github.com/FangcunMount/iam/actions/runs/31173648686) 证明
  `version=21, dirty=0`、两表均 `present=0` / `already_absent`，目标库表数从 22 降为 20，三张日志/审计表仍保留。
- `000022`：[专用生产只读预检](https://github.com/FangcunMount/iam/actions/runs/31177351752) 在 `version=21, dirty=0` 上证明
  `operation_logs/audit_logs/auth_token_audit` 精确行数均为 0，FK/trigger/view/routine/event 依赖均为 0；
  Performance Schema 表 I/O 不可用，依业务所有者批准的 `audit_tables` waiver 判定 eligible。
  [PR #7](https://github.com/FangcunMount/iam/pull/7) 的 MySQL 8 门禁覆盖正常删除、缺表幂等、View 依赖整批保留、
  down fail closed 和空库完整升级/ bootstrap；[发布前备份](https://github.com/FangcunMount/iam/actions/runs/31177693359) 时间戳为
  `20260807_201936`，大小 32, 748, 472 bytes。
  [main MySQL 8 复验](https://github.com/FangcunMount/iam/actions/runs/31177774248) 和
  [main CI](https://github.com/FangcunMount/iam/actions/runs/31177774149) 成功后，
  [生产发布](https://github.com/FangcunMount/iam/actions/runs/31178122870) merge SHA
  `29137d6d75de80992872db25cd0a53e686926b1e`；[发布后只读验收](https://github.com/FangcunMount/iam/actions/runs/31178521532) 证明
  `version=22, dirty=0`、三表均 `present=0` / `already_absent`，目标库表数从 20 降为 17，最新备份仍为 `20260807_201936`。
- AuthN 数据收敛：[完整备份](https://github.com/FangcunMount/iam/actions/runs/31554724693) `iam_backup_20260812_094657.sql.gz` 后，
  [format v5 apply](https://github.com/FangcunMount/iam/actions/runs/31555252130) 提交最后 49, 029 个 LoginIdentity；
  [独立 verify](https://github.com/FangcunMount/iam/actions/runs/31555345065) 和
  [当前生产 SHA 的 status](https://github.com/FangcunMount/iam/actions/runs/31557636673) 均证明 remaining login/password
  inserts 0、hard conflicts 0、`retirement_eligible=true`。merge SHA `f2dd3a5365cdc420dad4e1122d9408043f988478` 的
  [生产发布](https://github.com/FangcunMount/iam/actions/runs/31557353525)和
  [独立健康验收](https://github.com/FangcunMount/iam/actions/runs/31557636370)成功，`/healthz`、`/readyz` 均为 200。
  format v5 preflight 的[生产只读实测](https://github.com/FangcunMount/iam/actions/runs/31558403325)显示数据库 uptime 407, 003 秒，
  三条表级 I/O 路径均不可用、两张旧表对象依赖为 0。2026-08-12 业务所有者随后显式接受无法排除低频外部访问的剩余风险并授权 `authn_tables` waiver 与 `000023` 发布。
- `000023`：[PR #19](https://github.com/FangcunMount/iam/pull/19) 合并为 `02fefadf475e26595b88e9aea836dd2c8c869f76`；
  [发布前 fresh preflight](https://github.com/FangcunMount/iam/actions/runs/31560880387) 再次证明 remaining 0、hard conflict 0、
  对象依赖 0 和 owner waiver eligible，[完整备份](https://github.com/FangcunMount/iam/actions/runs/31561035653) 为
  `iam_backup_20260812_114507.sql.gz`、39, 187, 955 bytes。
  [首次部署](https://github.com/FangcunMount/iam/actions/runs/31561364222) 已加载该 SHA 的镜像 digest `sha256:9cc95491…`，
  但迁移内重复的逐行 AuthN 断言在 230k 行规模上长期运行并导致部署探针超时；后续只读状态证明 DROP 已提交而 version 仍为 dirty。事故恢复没有恢复旧表或回填 canonical 数据；在两表均不存在、
  canonical schema 完整且 migrate lock 空闲的门禁下，[受控收尾](https://github.com/FangcunMount/iam/actions/runs/31564064527) 只执行
  `schema_migrations.dirty: 1→0` 并启动容器。[发布后只读验收](https://github.com/FangcunMount/iam/actions/runs/31564108987) 证明
  `version=23, dirty=0`、两张旧表和遗留查询均为 0，目标库从 17 表降为 15 表；
  [完整生产健康验收](https://github.com/FangcunMount/iam/actions/runs/31564175435) 证明容器 healthy、`/healthz=200`、
  `/readyz=200` 且 MySQL/Redis 可达。
- `000024`：[完整 schema/内容/依赖取证](https://github.com/FangcunMount/iam/actions/runs/31598427721) 证明四表各 1, 359 行，
  两组扩展 checksum 分别为 698, 548, 972 / 385, 223, 237，双向 ID 差异和 canonical ID 重叠均为 0，FK/trigger/view/routine/event 依赖均为 0；
  [完整备份](https://github.com/FangcunMount/iam/actions/runs/31598515371) 为 `iam_backup_20260812_205205.sql.gz`、
  28, 686, 757 bytes。[PR #34](https://github.com/FangcunMount/iam/pull/34) 合并为
  `7e501470b0c9ba701764953cafa5db3ce7a0090c`，main [CI](https://github.com/FangcunMount/iam/actions/runs/31600598605)、
  [CodeQL](https://github.com/FangcunMount/iam/actions/runs/31600598637) 和
  [MySQL 8 迁移语义](https://github.com/FangcunMount/iam/actions/runs/31600598524)均成功；
  [生产发布](https://github.com/FangcunMount/iam/actions/runs/31600986986) 后，
  [独立数据库验收](https://github.com/FangcunMount/iam/actions/runs/31601472440) 证明 `version=24, dirty=0`、精确 15 张 BASE TABLE、
  14 张退役表和显式表级授权均为 0；[独立健康验收](https://github.com/FangcunMount/iam/actions/runs/31601566404) 证明容器 healthy、`/healthz=200`、
  `/readyz=200`、运行 SHA 匹配且 MySQL/Redis 可达。
- `000025`：[完整备份](https://github.com/FangcunMount/iam/actions/runs/32756675723) 为 `iam_backup_20260825_012703.sql.gz`、
  29, 210, 688 bytes；[deduplicate dry-run](https://github.com/FangcunMount/iam/actions/runs/32789984653) 锁定 100 个重复组、
  106 条冗余候选与 SHA-256 指纹，[apply](https://github.com/FangcunMount/iam/actions/runs/32790080743) 在备份名、数量、
  指纹和写锁下重算集合全部匹配后软删除 106 条冗余记录。[PR #36](https://github.com/FangcunMount/iam/pull/36) 发布 migration 25；
  [PR #37](https://github.com/FangcunMount/iam/pull/37) 修正复合索引的验收计数口径并把生产同款 preflight 加入 MySQL 8 full-chain 门禁。
  最终 SHA `c84c638d46ade0a2b1b65379289931ef9e28b172` 的
  [CI](https://github.com/FangcunMount/iam/actions/runs/32791721343)、
  [CodeQL](https://github.com/FangcunMount/iam/actions/runs/32791721416)、
  [MySQL 8](https://github.com/FangcunMount/iam/actions/runs/32791721351) 和
  [生产发布](https://github.com/FangcunMount/iam/actions/runs/32792113058)均成功；
  [数据库验收](https://github.com/FangcunMount/iam/actions/runs/32792508524) 证明 `version=25, dirty=0`、重复组为 0、guard 状态 `1/1`，
  [健康验收](https://github.com/FangcunMount/iam/actions/runs/32792503728) 证明运行 SHA 精确匹配、容器 healthy、
  health/readiness 为 200 且 MySQL/Redis 可达。
- `000026–000027`：[生产切换](https://github.com/FangcunMount/iam/actions/runs/32859067799) 使用备份
  `iam_backup_20260825_222411.sql.gz`，将 47 条旧 p 与 155 条旧 g 转换为 105 条 PermissionGrant 和 6 条 RoleInheritance，
  apply/verify/evidence 的规范化 hash 一致后执行不可逆退役。[发布后状态](https://github.com/FangcunMount/iam/actions/runs/32876762969) 证明
  `version=27, dirty=0`、16 张 BASE TABLE 精确匹配且 `casbin_rule`、`authz_cutover_state`、`scope_kinds` 均不存在；
  [Mac mini 备份克隆演练](https://github.com/FangcunMount/iam/actions/runs/32875449928) 独立重放完整 25→27 链并通过 artifact checksum。
  AuthZ v4 功能基线 SHA `d3f58369d8c58dbf50ae15282f5641bc370055a6` 的
  [生产发布](https://github.com/FangcunMount/iam/actions/runs/32877019508)与
  [独立健康验收](https://github.com/FangcunMount/iam/actions/runs/32877567211)成功。
- `000028`：[发布前完整备份](https://github.com/FangcunMount/iam/actions/runs/32924685068) 为
  `iam_backup_20260826_105831.sql.gz`、29, 278, 064 bytes；
  [只读预检](https://github.com/FangcunMount/iam/actions/runs/32924963311) 在 `version=27` 上证明非法/未规范行、跨 User 冲突组和重复组/额外行均为 0，
  索引尚未存在。[PR #57](https://github.com/FangcunMount/iam/pull/57) 合并为功能发布 SHA `9fab2d674a5d0e11e11dbd8a24098aba8b1c7851`；
  main [CI](https://github.com/FangcunMount/iam/actions/runs/32925286458)、
  [CodeQL](https://github.com/FangcunMount/iam/actions/runs/32925286150) 和
  [MySQL 8 并发门禁](https://github.com/FangcunMount/iam/actions/runs/32925286430) 成功后，
  [生产部署](https://github.com/FangcunMount/iam/actions/runs/32925583337) 完成。
  [发布后 status](https://github.com/FangcunMount/iam/actions/runs/32925876593) 证明 `version=28, dirty=0`、
  16 张 BASE TABLE 精确匹配；[发布后 guard](https://github.com/FangcunMount/iam/actions/runs/32926175510) 证明非法/未规范行 0、
  跨 User 冲突组 0、重复组/额外行 `0/0`、唯一索引计数 1；[独立健康验收](https://github.com/FangcunMount/iam/actions/runs/32925995472) 证明容器
  healthy、health/readiness 200、运行 SHA 精确匹配且 MySQL/Redis 可达。

`000020–000028` 的仓库实现与目标库执行均已完成。后续不得改写已发布 migration，若发现遗留问题必须新增 forward migration。

## 11. 台账维护规则

每完成一个候选项，在对应行补充：实施 PR/commit、删除或保留决定、验证命令、运行环境、证据窗口和残余风险。若实现改变模块事实，应同步对应 canonical 文档：

- MySQL/迁移：[MySQL、Unit of Work 与数据库迁移](../03-基础设施/01-MySQL事务与迁移.md)
- Redis/缓存：[Redis 与缓存一致性](../03-基础设施/02-Redis与缓存一致性.md)
- 事件：[事件目录、发布语义与 Transactional Outbox](../03-基础设施/03-事件与Transactional-Outbox.md)
- 接口/SDK：[REST、gRPC 与契约治理](../04-接口与SDK/01-REST-gRPC与契约治理.md)
- 迁移发布：[迁移、发布与数据库运维](03-迁移发布与数据库运维.md)

本文记录退役决策和证据，不替代上述主题的设计与运行语义。
