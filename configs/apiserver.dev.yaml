# ============================================================================
# iam-apiserver 开发环境配置
# ============================================================================
# 用途：IAM 身份认证与授权服务本地开发环境配置
# 维护：开发团队
# 更新时间：2025-12-10
# ============================================================================

# ============================================================================
# 1. 服务运行模式
# ============================================================================
server:
  mode: "debug"  # debug, test, release

# ============================================================================
# 2. 服务端口配置
# ============================================================================

# ----------------------------------------------------------------------------
# 2.1 RESTful HTTP/HTTPS 服务配置
# ----------------------------------------------------------------------------
# HTTP REST API 配置（不安全，内网使用）
# 用途：本地开发调试，健康检查
insecure:
  bind-address: "0.0.0.0"
  bind-port: 18081

# HTTPS REST API 配置（安全，外网访问）
# 用途：本地开发 HTTPS 测试
secure:
  bind-address: "0.0.0.0"
  bind-port: 18441
  tls:
    cert-file: /data/infra/ssl/web/iam-apiserver.crt
    private-key-file: /data/infra/ssl/web/iam-apiserver.key

# CORS 跨域配置（开发环境允许所有来源）
cors:
  allowed-origins:
    - "*"
  allowed-methods:
    - "GET"
    - "POST"
    - "PUT"
    - "DELETE"
    - "OPTIONS"
    - "PATCH"
  allowed-headers:
    - "Content-Type"
    - "Authorization"
    - "X-Requested-With"

# ----------------------------------------------------------------------------
# 2.2 gRPC 服务配置（开发环境）
# ----------------------------------------------------------------------------
grpc:
  authz-assignment-constraints-file: configs/grpc_assignment_constraints.yaml
  bind-address: "0.0.0.0"
  bind-port: 19091
  healthz-port: 19092
  
  # mTLS 双向认证配置（由 infra 项目统一管理）
  mtls:
    enabled: true   # 开发环境开启 mTLS
    cert-file: "/data/infra/ssl/grpc/server/iam-apiserver.crt"
    key-file: "/data/infra/ssl/grpc/server/iam-apiserver.key"
    ca-file: "/data/infra/ssl/grpc/ca/ca-chain.crt"
    # ca-dir: "/data/infra/ssl/grpc/ca/"  # 可选：CA 证书目录，支持多级 CA 信任链
    require-client-cert: true
    
    # 允许的客户端证书 OU 列表
    allowed-ous:
      - QS
      - Admin
      - Ops
    
    # 证书自动重载
    enable-auto-reload: true
    reload-interval: 5m
  
  # 应用层凭证认证配置
  auth:
    enabled: false  # 开发环境默认禁用
    enable-bearer: true   # JWT Bearer Token
    enable-hmac: true     # HMAC 签名
    enable-api-key: true  # API Key
    hmac-timestamp-validity: 5m
    require-identity-match: true
  
  # 服务级 ACL 配置
  acl:
    enabled: true   # 服务身份必须通过 ACL
    config-file: "./configs/grpc_acl.yaml"
  
  # 审计日志配置
  audit:
    enabled: true  # 开发环境开启审计日志，便于调试

# ============================================================================
# 2.3 Token 配置
# ============================================================================
auth:
  jwt_issuer: "https://iam.fangcunmount.cn"
  access_token_audience:
    - "qs-api"
    - "collection-api"
  access_token_ttl: 15m
  refresh_token_ttl: 168h
  session_max_ttl: 24h
  password_lockout:
    enabled: false
    threshold: 5
    lock_duration: 15m

identity:
  session_revocation:
    poll_interval: 2s
    batch_size: 50
    retry_base_delay: 5s
    retry_max_delay: 5m
    stale_processing_after: 1m

health:
  readiness:
    component_timeout: 500ms
    total_timeout: 2s
    outbox_max_pending_age: 5m
    drain_delay: 0s

# ============================================================================
# 3. 数据存储配置
# ============================================================================

# ----------------------------------------------------------------------------
# 3.1 MySQL 数据库配置
# ----------------------------------------------------------------------------
mysql:
  host: "127.0.0.1:3306"
  username: "infra_dev"        # 对应 build/docker/infra/dev.env -> MYSQL_USER
  password: "dev_pass_123"     # 对应 build/docker/infra/dev.env -> MYSQL_PASSWORD
  database: "iam"
  max-idle-conns: 10
  max-open-conns: 100
  conn-max-lifetime: 3600  # 秒
  log-level: 4 # GORM log level, 1: silent, 2:error, 3:warn, 4:info (开发环境使用 info)

# ----------------------------------------------------------------------------
# 3.2 数据库迁移配置
# ----------------------------------------------------------------------------
migration:
  enabled: true  # 开发环境启用自动迁移
  database: "iam"

# ----------------------------------------------------------------------------
# 3.3 Redis 配置
# ----------------------------------------------------------------------------
redis:
  # Redis - 用于缓存、令牌等
  cache:
    host: "127.0.0.1"
    port: 6379
    username: "app"  # 开发环境默认使用 default 用户
    password: "dev_app_123"  # 对应 build/docker/infra/dev.env -> REDIS_CACHE_PASSWORD/REDIS_AUTH_APP_PASSWORD
    database: 0  # Cache 使用 DB 0
    max-idle: 200
    max-active: 500
    min-idle-conns: 20
    timeout: 10
    pool-timeout: 10
    dial-timeout: 5
    read-timeout: 5
    write-timeout: 5
    enable-cluster: false
    use-ssl: false
    enable-logging: true  # 开启 Redis 命令日志（用于调试，生产环境建议关闭）

# ============================================================================
# 4. 安全与认证配置
# ============================================================================

# ----------------------------------------------------------------------------
# 4.1 JWKS 密钥管理
# ----------------------------------------------------------------------------
jwks:
  keys_dir: "./configs/keys"  # 私钥存放目录（相对或绝对路径）
  auto_init: true           # 启动时若无 active key 则自动生成（仅建议在开发环境启用）
  rotation:
    automatic_enabled: false
    check_cron: "@every 1h"
    rotation_interval: 720h
    grace_period: 168h
    max_publishable_keys: 3

# ----------------------------------------------------------------------------
# 4.2 TLS 证书（通用）
# ----------------------------------------------------------------------------
tls:
  cert: /data/infra/ssl/web/iam-apiserver.crt
  key: /data/infra/ssl/web/iam-apiserver.key
  strict: false  # 开发环境不严格验证

# ----------------------------------------------------------------------------
# 4.3 IDP 身份提供商
# ----------------------------------------------------------------------------
idp:
  encryption-key: ""  # 留空使用默认密钥（开发环境）或通过环境变量 IAM_APISERVER_IDP_ENCRYPTION_KEY 设置
  wecom:
    agent_id: ""  # 企业微信应用 AgentID；登录请求只传 corp_id/auth_code，服务端补齐 agent_id/corp_secret
  wechat_open:
    app_id: ""  # 微信开放平台网站应用 AppID；扫码登录/绑定由服务端注入
    login_redirect_uri: ""  # 扫码登录回调地址
    link_redirect_uri: ""  # 登录身份绑定回调地址

# ----------------------------------------------------------------------------
# 4.4 短信登录 OTP（发码）
# ----------------------------------------------------------------------------
# provider: log（仅打日志）| mq（经 NSQ 发布 iam.notify.sms，由下游真正发短信）| aliyun（直连阿里云短信）
# mq 需同时开启下方 nsq.enabled
sms:
  provider: log
  login_otp_ttl: 5m
  login_otp_send_cooldown: 60s
  login_otp_code_length: 6
  login_otp_max_attempts: 5
  # 限量：单号码+场景滑动窗口发送上限。-1 关闭，0 取默认（小时 5、每天 10）。
  login_otp_hourly_limit: 5
  login_otp_daily_limit: 10
  aliyun:
    # 号码认证服务（Dypns）SendSmsVerifyCode；个人开发者免短信资质。
    # 推荐：留空 access_key_id/access_key_secret，走阿里云默认凭据链
    # （ENV: ALIBABA_CLOUD_ACCESS_KEY_ID/SECRET，或 RAM 角色/ECS 元数据）。
    access_key_id: ""
    access_key_secret: ""
    sign_name: ""
    template_code: ""
    endpoint: dypnsapi.aliyuncs.com
    code_param_name: code
    min_param_name: min
    timeout_millis: 10000

# ----------------------------------------------------------------------------
# 4.5 Suggest 联想模块
# ----------------------------------------------------------------------------
suggest:
  enable: true
  full_sync_cron: "@every 1h"
  # 默认 Delta 含活跃 upsert + 软删 tombstone；无 CDC 时仍建议留空，仅全量刷新。
  delta_sync_cron: ""
  max_results: 20
  key_pad_len: 25
  # Trie 通配符展开的最大终端键数；0=领域默认 100。
  # trie_wildcard_key_cap: 200
  # 按 operator 限流；默认关闭（全零）。backend: memory（单进程令牌桶）| redis（多副本共享固定窗口，约 1s）。
  # rate_limit:
  #   per_operator_qps: 30
  #   per_operator_burst: 60
  #   mobile_keyword_per_operator_qps: 6
  #   mobile_keyword_per_operator_burst: 12
  #   backend: redis
  #   operator_map_max_entries: 10000
  # visibility_cache_ttl_seconds: 60
  # 占位 org_id（业务组织）；tenant 授权域见 JWT tenant_id=fangcun，勿与 org 混用。
  # 负责人维度来自 profiles.created_by；多组织生产请配置 full_sql。
  loader_placeholder_org_id: 1

# ----------------------------------------------------------------------------
# 4.6 调试治理接口
# ----------------------------------------------------------------------------
debug:
  cache_governance:
    enabled: true
    require_admin: false

# ============================================================================
# 5. 系统运行配置
# ============================================================================

# ----------------------------------------------------------------------------
# 5.1 日志配置（开发环境 - 使用 component-base v0.2.9 duplicate 模式）
# ----------------------------------------------------------------------------
log:
  # 基础配置
  disable-caller: false        # 开发环境显示调用者信息
  disable-stacktrace: false    # 开发环境显示堆栈
  level: debug                 # 开发环境使用 debug 级别
  format: console              # 控制台格式（console/json）
  enable-color: true           # 🎨 开发环境启用彩色输出，便于调试
  development: true            # 开发模式
  
  # 📊 日志轮转配置（按大小）
  max-size: 100                # 单个日志文件最大大小（MB）
  max-age: 7                   # 保留 7 天
  max-backups: 10              # 保留 10 个备份文件
  compress: true               # 压缩旧文件
  
  # ⏰ 按时间轮转（可选，与大小轮转二选一）
  enable-time-rotation: false  # 开发环境暂不启用
  time-rotation-format: "2006-01-02"  # 按天轮转格式
  
  # 🎯 分级输出（v0.2.9 duplicate 模式）
  enable-level-output: true       # 启用分级输出
  level-output-mode: "duplicate"  # 输出模式：duplicate=重复输出（推荐）
  level-output-paths:
    all:
      - stdout                    # 所有日志输出到控制台（彩色，开发环境）
      - logs/iam-apiserver.log    # 所有日志记录到文件（完整日志）
    warn:
      - logs/iam-warn.log         # WARN 以上额外记录，辅助排查性能/配置问题
    error:
      - logs/iam-error.log        # ERROR 额外落盘，便于快速定位故障

# ----------------------------------------------------------------------------
# 5.2 NSQ 消息队列配置（可选，用于事件驱动）
# ----------------------------------------------------------------------------
nsq:
  enabled: false                              # 开发环境默认禁用
  lookupd-addrs:                              # NSQLookupd 地址
    - "127.0.0.1:4161"
  nsqd-addr: "127.0.0.1:4150"                # NSQd 地址
  max-attempts: 5                             # 消息最大重试次数
  max-in-flight: 200                          # 最大并发处理数
  msg-timeout: 60                             # 消息超时时间（秒）
  requeue-delay: 5                            # 重新入队延迟（秒）

# ----------------------------------------------------------------------------
# 5.3 事件目录与 Transactional Outbox Relay
# ----------------------------------------------------------------------------
events:
  catalog_path: configs/events.yaml
  outbox_relay_interval: 2s
  outbox_relay_batch_size: 50
  outbox_relay_retry_delay: 10s

# Policy confirmation budgets; no database reads occur on the Check path.
authz:
  policy-sync:
    check-interval: 10s
    sync-timeout: 10s
    max-unconfirmed: 60s
  attribute-providers-file: configs/authz_attribute_providers.yaml
