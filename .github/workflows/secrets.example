# GitHub Secrets 配置模板

# 复制此文件并根据实际情况填写
# 然后在 GitHub 仓库设置中添加这些 Secrets
# Settings -> Secrets and variables -> Actions -> New repository secret

# =============================================================================
# Docker Hub 配置
# =============================================================================
DOCKER_USERNAME=your_dockerhub_username
DOCKER_PASSWORD=your_dockerhub_token_or_password

# =============================================================================
# 开发环境配置 (dev)
# =============================================================================
# API URL
DEV_API_URL=http://dev.yourdomain.com:8080

# 数据库配置
DB_HOST_dev=localhost
DB_PORT_dev=3306
DB_NAME_dev=iam_dev
DB_USER_dev=root
DB_PASSWORD_dev=your_dev_password

# 服务器 SSH 配置（用于健康检查）
SERVER_HOST_dev=dev.yourdomain.com
SERVER_USER_dev=deploy
SSH_PRIVATE_KEY_dev=-----BEGIN RSA PRIVATE KEY-----
...
-----END RSA PRIVATE KEY-----

# =============================================================================
# 预发布环境配置 (staging)
# =============================================================================
# API URL
STAGING_API_URL=http://staging.yourdomain.com:8080

# 数据库配置
DB_HOST_staging=staging-db.yourdomain.com
DB_PORT_staging=3306
DB_NAME_staging=iam_staging
DB_USER_staging=iam_user
DB_PASSWORD_staging=your_staging_password

# 服务器 SSH 配置
SERVER_HOST_staging=staging.yourdomain.com
SERVER_USER_staging=deploy
SSH_PRIVATE_KEY_staging=-----BEGIN RSA PRIVATE KEY-----
...
-----END RSA PRIVATE KEY-----

# =============================================================================
# 生产环境配置 (prod)
# =============================================================================
# API URL
PROD_API_URL=https://api.yourdomain.com

# 数据库配置
DB_HOST_prod=prod-db.yourdomain.com
DB_PORT_prod=3306
DB_NAME_prod=iam_production
DB_USER_prod=iam_user
DB_PASSWORD_prod=your_production_password

# 生产环境通常不直接 SSH，而是通过 CD 工具或 Kubernetes
# 如果需要，可以配置：
# SERVER_HOST_prod=prod.yourdomain.com
# SERVER_USER_prod=deploy
# SSH_PRIVATE_KEY_prod=...

# =============================================================================
# 可选配置
# =============================================================================

# Codecov Token (用于代码覆盖率报告)
CODECOV_TOKEN=your_codecov_token

# Slack Webhook (用于通知)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# 其他第三方服务 Token
# SENTRY_DSN=...
# DATADOG_API_KEY=...

# =============================================================================
# 注意事项
# =============================================================================
# 1. 不要将此文件提交到 Git 仓库
# 2. 使用强密码和定期轮换
# 3. 为每个环境使用不同的凭据
# 4. SSH 私钥需要包含完整的 BEGIN 和 END 标记
# 5. 生产环境建议使用额外的安全措施（如 IP 白名单、VPN 等）
